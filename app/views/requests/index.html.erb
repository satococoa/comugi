<h1>会いたい一覧</h1>
<% @requests.each do |request| %>
  <%=render file: 'shared/_user', locals: {user: request.target(users: @users)}%>
  <%=link_to '解除', request, :confirm => 'Are you sure?', :method => :delete %>
<% end %>
